# Backend API Configuration
# Used by the Expo app to connect to the backend API
EXPO_PUBLIC_API_URL=http://localhost:3000/api

# Backend Port (for Docker compose)
BACKEND_PORT=3000

# Backend Environment
NODE_ENV=development
