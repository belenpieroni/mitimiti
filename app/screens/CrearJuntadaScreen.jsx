import { View, Text } from 'react-native';
import { colors } from '../theme/colors';

export default function CrearJuntadaScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: colors.background }}>
      <Text>Crear Juntada</Text>
    </View>
  );
}